// npm package requirements to generate package.json dynamically.
// This map will be converted to JSON.
ext.packageInfo = [
    // We need this to suppress npm's warning messages.
    'private': true,
    // To keep modules' version consistent, we specify version without ^, >=, etc.
    'devDependencies': [
        'bower': '1.7.6',
        'request': '2.53.0',
        'coffee-script': '1.10.0',
        'uglify-js': '2.3.6',
        'source-map': '0.4.4',
        'graceful-fs': '3.0.8',
        'inflight': '1.0.4',
        'inherits': '2.0.1',
        'ansi-regex': '2.0.0',
        'core-util-is': '1.0.1',
        'mime': '1.3.4',
        'osenv': '0.1.3',
        'less': '2.5.3',
        'q': '1.4.1',
        'glob': '4.5.3',
        'browserify': '12.0.1',
        'licensify': '2.2.0',
        'mocha': '2.4.5',
    ],
    'scripts': [
        'browserify': [
            'browserify',
            // Keep license comments in the bundled JavaScript file.
            '-p licensify',
            // Suppress errors for missing internal modules for trireme (gradle-console).
            '--im',
            // To avoid some global vars to be replaced, set noparse options.
            '--noparse=./node_modules/amdefine/amdefine.js',
            '--noparse=./node_modules/bower/lib/lib/commands/help.js',
            '--noparse=./node_modules/bower/lib/lib/util/template.js',
            '--noparse=./node_modules/bower/lib/node_modules/amdefine/amdefine.js',
            '--noparse=./node_modules/bower/lib/node_modules/fs-write-stream-atomic/index.js',
            '--noparse=./node_modules/bower/lib/node_modules/graceful-fs/fs.js',
            '--noparse=./node_modules/graceful-fs/fs.js',
            '--noparse=./node_modules/mocha/lib/mocha.js',
            // This is "require"d but we don't need this.
            '--ignore=opn',
            '--node',
            'build.js',
            '-o outputs/build.tmp.js',
        ].join(' '),
        'uglify': [
            'uglifyjs',
            'outputs/build.tmp.js',
            // Preserve license comments.
            '--comments',
            // This file is the final artifact to bundle.
            '-o outputs/build.js',
        ].join(' '),
    ]
]
